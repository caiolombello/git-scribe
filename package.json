{
  "name": "git-scribe",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "git-scribe": "dist/cli.js"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --target=node --outdir dist",
    "build:bun": "bun build src/cli.ts --target=bun --outdir dist",
    "build:node": "bun build src/cli.ts --target=node --outdir dist",
    "test": "bun test"
  }
}
